# Copy to .env for local overrides (this file is only documentation).
# Local dev needs no setup: the app falls back to http://localhost:8000.
# For a hosted deploy, point this at the live backend URL instead:
# VITE_API_URL=https://aakaar-backend.onrender.com
VITE_API_URL=http://localhost:8000
